(function(angular) {
    'use strict';

    angular.module( "app" )
        .controller( "session", session );

        // **************************************
        // Private construction function
        // **************************************

        function session( logger )
        {
            var vm = this;
        }

})(this.angular);
